# ToDos

1. Add basic spell checking
2. store multiple prompts in settings. in addtion to the prompt store a name, and the langauge model that it is for, will 'all' being an option. 
3. When a file is saved, store the path of the folder it was saved to to settngs and use that folder as the default for the next save.
4. Add elements to the ui to allow the LLM Prompt to be eselected from those stored in settings.
5. add a splash screen to display during program start
6. Add ollama url to settings.
